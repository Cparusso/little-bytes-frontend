import React from "react";
import styled from "styled-components";

const About = () => {
  return <AboutWrapper>About us page coming soon...</AboutWrapper>;
};

export default About;

const AboutWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
`;
